name: Test Github Action
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Set up Git repository
        uses: sikos/thesis@master
      - uses: xu-cheng/latex-action@v2
        with:
          root_file: dissertation.tex
